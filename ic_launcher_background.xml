// =============================================================
// Rigelsan MSDK Test — Settings
// =============================================================

pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        // DJI MSDK V5 artık Maven Central'da, ek repo gerekmiyor
    }
}

rootProject.name = "RigelsanMSDK"
include(":app")
